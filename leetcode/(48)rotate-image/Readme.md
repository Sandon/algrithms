# 
对于一个点而言，它的坐标转换规则如下：
(i, j) => (j, n - 1 - i)

那么找到一个开始节点后，转换到新位置，此位置的原来节点再到它的新位置，以此类推，只需要4次这4个节点就都找到了新位置；

那么对于一个圈而言，只需要找到 {边长 - 1} 个节点作为开始节点做上述转换；

然后再看有多少个圈；

总结就是：
    for 循环圈数
        for 循环圈的 {边长 - 1} 的节点
            以上述节点作为开始节点，对4个节点进行位置转换

